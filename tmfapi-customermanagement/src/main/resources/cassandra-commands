can not use batch with "IF" in queries --> Cassandra: Batch with conditions cannot span multiple tables
can't use  IN(id list) with IF in query , so have to generate a query list --> IN on the partition key is not supported with conditional deletions

CassandraUpdateQueryGenerator - line (\63-73)
superior parent id should be removed from super map in attribute row..to do that need to check whether all relevant children of a super parent are removed from
immediate map at the end. it is not implemented.  but no impact.

if an class has same object as two properties have to define two tables..other wise cant set value to correct field when retrieving
main entity tables
child entity tables
tables with attribute values as primary ids

 insert into customerDto(id,attributes,childentity) values('1111',{'222':'222','2222':'333'},{'child':'222'});
  update startdatetime set superiorparent = superiorparent + {'ccc':'333'} where id = '333ppppp';
  delete from startdatetime where id in('3333') if immediateparent = {};
  delete from startdatetime where id = '333ppppp' IF immediateparent = {};
update customerDto SET childentity = childentity + {'11111':'5555'},attributes = attributes +{'111999':'999'} where id = '88888qqqq';
select * from price where superiorparent contains 'Test-Object-01' ALLOW FILTERING;//queries with allow filtering not used in logic

CREATE TABLE IF NOT EXISTS customerDto(id text PRIMARY KEY,childentity map<text,frozen <list<text>>>,attributes map<text,text>);


create keyspace if not exists tmf_db with replication = {'class':'SimpleStrategy','replication_factor':1};
CREATE TABLE IF NOT EXISTS customerDto(id text PRIMARY KEY,childentity map<text,text>,attributes map<text,text>);
CREATE TABLE CREATE TABLE IF NOT EXISTS accountref(primaryId text PRIMARY KEY,attributes map<text,text>,immediateparent map<text,text>,superiorparent map<text,text>);
             CREATE TABLE IF NOT EXISTS agreementref(primaryId text PRIMARY KEY,attributes map<text,text>,immediateparent map<text,text>,superiorparent map<text,text>);
             CREATE TABLE IF NOT EXISTS characteristicDto(primaryId text PRIMARY KEY,attributes map<text,text>,immediateparent map<text,text>,superiorparent map<text,text>);
             CREATE TABLE IF NOT EXISTS contactmedium(primaryId text PRIMARY KEY,attributes map<text,text>,immediateparent map<text,text>,superiorparent map<text,text>);
             CREATE TABLE IF NOT EXISTS creditprofile(primaryId text PRIMARY KEY,attributes map<text,text>,immediateparent map<text,text>,superiorparent map<text,text>);
             CREATE TABLE IF NOT EXISTS engagedparty(primaryId text PRIMARY KEY,attributes map<text,text>,immediateparent map<text,text>,superiorparent map<text,text>);
             CREATE TABLE IF NOT EXISTS mediumcharacteristic(primaryId text PRIMARY KEY,attributes map<text,text>,immediateparent map<text,text>,superiorparent map<text,text>);
             CREATE TABLE IF NOT EXISTS paymentref(primaryId text PRIMARY KEY,attributes map<text,text>,immediateparent map<text,text>,superiorparent map<text,text>);
             CREATE TABLE IF NOT EXISTS relatedparty(primaryId text PRIMARY KEY,attributes map<text,text>,immediateparent map<text,text>,superiorparent map<text,text>);
             CREATE TABLE IF NOT EXISTS timeperiod(primaryId text PRIMARY KEY,attributes map<text,text>,immediateparent map<text,text>,superiorparent map<text,text>);


CREATE TABLE IF NOT EXISTS customerDto(id text PRIMARY KEY, statusReason text,href text,type text);
CREATE TABLE IF NOT EXISTS customerDto(id text PRIMARY KEY, statusreason text,href text,type text,schemalocation text, name text,basetype text,status text);



CREATE TABLE IF NOT EXISTS mediumCharacteristic(parentEntity text,parentId text,primaryId text PRIMARY KEY,country text,city text,schemaLocation text,contactType text,type text,baseType text,socialNetworkId text,emailAddress text,phoneNumber text,stateOrProvince text,faxNumber text,postCode text,street1 text,street2 text);
CREATE TABLE IF NOT EXISTS contactMediumDto(parentEntity text,parentId text,primaryId text PRIMARY KEY,startDateTime text,endDateTime text,schemaLocation text,mediumType text,type text,preferred boolean,baseType text);
CREATE TABLE IF NOT EXISTS creditProfileDto(parentEntity text,parentId text,primaryId text PRIMARY KEY,creditProfileDate text,creditScore int,startDateTime text,endDateTime text,schemaLocation text,creditRiskRating int,type text,baseType text);
CREATE TABLE IF NOT EXISTS agreementRef(parentEntity text,parentId text,primaryId text PRIMARY KEY,referredType text,schemaLocation text,name text,id text,href text,type text,baseType text);
CREATE TABLE IF NOT EXISTS relatedPartyDto(parentEntity text,parentId text,primaryId text PRIMARY KEY,referredType text,role text,schemaLocation text,name text,id text,href text,type text,baseType text );
CREATE TABLE IF NOT EXISTS characteristicDto(parentEntity text,parentId text,primaryId text PRIMARY KEY,schemaLocation text,valueType text,name text,type text,baseType text);
CREATE TABLE IF NOT EXISTS paymentRef(parentEntity text,parentId text,primaryId text PRIMARY KEY,referredType text,schemaLocation text,name text,id text,href text,type text,baseType text);
CREATE TABLE IF NOT EXISTS account(parentEntity text,parentId text,primaryId text PRIMARY KEY,referredType text,schemaLocation text,name text,description text,id text,href text,type text,baseType text);
CREATE TABLE IF NOT EXISTS engagedPartyDto(parentEntity text,parentId text,primaryId text PRIMARY KEY,referredType text,role text,schemaLocation text,name text,id text,href text,type text,baseType text);
CREATE TABLE IF NOT EXISTS customerDto(startDateTime text,endDateTime text,schemaLocation text,type text,baseType text,statusReason text,name text,id text PRIMARY KEY,href text,status text);